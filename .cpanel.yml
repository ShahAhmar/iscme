---
deployment:
  tasks:
    - cd /home2/gaftimco/iscme.gaftim.com && /usr/bin/git fetch origin && /usr/bin/git reset --hard origin/main
    - /usr/bin/php /home2/gaftimco/iscme.gaftim.com/artisan optimize:clear 2>/dev/null || true