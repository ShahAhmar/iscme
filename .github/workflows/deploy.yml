name: Deploy to Live Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger server pull
        run: |
          curl -s --max-time 30 https://iscme.gaftim.com/pull.php
          echo "✅ Deployment triggered!"
