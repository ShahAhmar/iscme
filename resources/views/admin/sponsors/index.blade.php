@extends('layouts.admin')
@section('title', 'Sponsors')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><p class="text-uppercase small fw-bold mb-1" style="letter-spacing:.12em;color:#1a73e8;">Conference content</p><h1 class="h3 mb-0">Sponsors & partners</h1></div><a class="btn btn-primary" href="{{ route('admin.sponsors.create') }}"><i class="bi bi-plus-lg me-1"></i>Add sponsor</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th class="ps-3">Sponsor</th><th>Tier</th><th>Website</th><th>Visibility</th><th class="text-end pe-3">Actions</th></tr></thead><tbody>@forelse($sponsors as $sponsor)<tr><td class="ps-3"><div class="d-flex align-items-center gap-3">@if($sponsor->logo)<img src="{{ Storage::url($sponsor->logo) }}" alt="" class="border rounded object-fit-contain p-1" width="56" height="40">@endif<strong>{{ $sponsor->name }}</strong></div></td><td>{{ $sponsor->tier }}</td><td>@if($sponsor->website_url)<a href="{{ $sponsor->website_url }}" target="_blank" rel="noopener">Visit</a>@endif</td><td><span class="badge text-bg-{{ $sponsor->is_published ? 'success' : 'secondary' }}">{{ $sponsor->is_published ? 'Published':'Draft' }}</span></td><td class="text-end pe-3"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.sponsors.edit',$sponsor) }}">Edit</a>@if(auth()->user()->hasRole(['super_admin','admin']))<form class="d-inline" method="POST" action="{{ route('admin.sponsors.destroy',$sponsor) }}" onsubmit="return confirm('Delete this sponsor?');">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No sponsors added yet.</td></tr>@endforelse</tbody></table></div>@if($sponsors->hasPages())<div class="card-body border-top">{{ $sponsors->links() }}</div>@endif</div>
@endsection
