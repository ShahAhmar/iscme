import React, { useMemo, useState } from 'react';
import { Head, router, usePage } from '@inertiajs/react';
import { ExternalLink, FileText, Plus, Search, Settings2, Sparkles, Trash2 } from 'lucide-react';
import AdminLayout from '../../../Admin/AdminLayout';

export default function PagesIndex({ pages }) {
    const [query, setQuery] = useState(''); const user = usePage().props.auth?.user;
    const filtered = useMemo(() => pages.filter(p => `${p.title} ${p.slug}`.toLowerCase().includes(query.toLowerCase())), [pages, query]);
    const destroy = page => { if (confirm(`Delete “${page.title}”? This cannot be undone.`)) router.delete(`/admin/pages/${page.id}`); };
    return <><Head title="Pages" /><div className="mb-7 flex flex-wrap items-end justify-between gap-4"><div><p className="mb-2 text-xs font-extrabold uppercase tracking-[.15em] text-blue-600">Website</p><h1 className="text-3xl font-black tracking-tight text-slate-900">Pages</h1><p className="mt-2 text-sm text-slate-500">Design, organise and publish your website content.</p></div><a href="/admin/pages/create" className="inline-flex items-center gap-2 rounded-xl bg-[#0d4c86] px-4 py-3 text-sm font-bold text-white no-underline shadow-lg shadow-blue-900/15 transition hover:-translate-y-0.5"><Plus size={17} />New page</a></div>
        <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 px-5 py-4"><div><h2 className="font-extrabold text-slate-800">All pages <span className="ml-1 text-sm font-medium text-slate-400">({pages.length})</span></h2></div><label className="relative block"><Search className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={16}/><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search pages" className="w-64 rounded-xl border border-slate-200 py-2 pl-9 pr-3 text-sm outline-none transition focus:border-blue-400 focus:ring-4 focus:ring-blue-50" /></label></div><div className="overflow-x-auto"><table className="min-w-full text-left"><thead className="bg-slate-50 text-[10px] font-extrabold uppercase tracking-[.12em] text-slate-400"><tr><th className="px-5 py-3">Page</th><th className="px-5 py-3">URL</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Updated</th><th className="px-5 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(page => <tr key={page.id} className="transition hover:bg-blue-50/35"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-50 text-blue-700"><FileText size={17}/></span><div><strong className="block text-sm text-slate-800">{page.title}</strong><span className="text-xs text-slate-400">Website page</span></div></div></td><td className="px-5 py-4"><code className="rounded bg-slate-100 px-2 py-1 text-xs text-slate-500">/{page.slug === 'home' ? '' : page.slug}</code></td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${page.is_published ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700'}`}>{page.is_published ? 'Published' : 'Draft'}</span></td><td className="px-5 py-4 text-sm text-slate-500">{new Date(page.updated_at).toLocaleDateString()}</td><td className="px-5 py-4"><div className="flex justify-end gap-1"><a href={`/admin/pages/${page.id}/builder`} title="Open visual builder" className="grid h-9 w-9 place-items-center rounded-lg text-blue-700 transition hover:bg-blue-50"><Sparkles size={17}/></a><a href={`/admin/pages/${page.id}/edit`} title="Page settings" className="grid h-9 w-9 place-items-center rounded-lg text-slate-500 transition hover:bg-slate-100"><Settings2 size={17}/></a><a href={`/${page.slug === 'home' ? '' : page.slug}`} target="_blank" title="View page" className="grid h-9 w-9 place-items-center rounded-lg text-slate-500 transition hover:bg-slate-100"><ExternalLink size={17}/></a>{['super_admin','admin'].includes(user?.role) && <button onClick={() => destroy(page)} title="Delete page" className="grid h-9 w-9 place-items-center rounded-lg text-rose-600 transition hover:bg-rose-50"><Trash2 size={17}/></button>}</div></td></tr>)}{filtered.length === 0 && <tr><td colSpan="5" className="px-5 py-14 text-center text-sm text-slate-400">No matching pages found.</td></tr>}</tbody></table></div></section></>;
}
PagesIndex.layout = page => <AdminLayout>{page}</AdminLayout>;
