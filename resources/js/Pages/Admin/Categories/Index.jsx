import React, { useState } from 'react';
import { Head, router, useForm, usePage } from '@inertiajs/react';
import { Edit3, FolderTree, Plus, Save, Trash2, X } from 'lucide-react';
import AdminLayout from '../../../Admin/AdminLayout';

export default function CategoriesIndex({ categories }) {
    const [editing, setEditing] = useState(null); const user = usePage().props.auth?.user;
    const form = useForm({ name: '', slug: '', description: '' });
    const open = category => { setEditing(category); form.setData({ name: category?.name || '', slug: category?.slug || '', description: category?.description || '' }); form.clearErrors(); };
    const submit = e => { e.preventDefault(); const options = { preserveScroll: true, onSuccess: () => { setEditing(null); form.reset(); } }; editing ? form.put(`/admin/categories/${editing.id}`, options) : form.post('/admin/categories', options); };
    const remove = category => { if (confirm(`Delete “${category.name}”? Its posts will become uncategorised.`)) router.delete(`/admin/categories/${category.id}`, { preserveScroll: true }); };
    return <><Head title="Categories"/><div className="mb-7"><p className="mb-2 text-xs font-extrabold uppercase tracking-[.15em] text-blue-600">Publishing</p><h1 className="text-3xl font-black tracking-tight text-slate-900">Categories</h1><p className="mt-2 text-sm text-slate-500">Organise news posts and build focused content feeds.</p></div><div className="grid gap-5 lg:grid-cols-[380px_1fr]"><section className="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-5 flex items-center justify-between"><h2 className="font-extrabold text-slate-800">{editing ? 'Edit category' : 'Add category'}</h2>{editing && <button onClick={() => {setEditing(null);form.reset();}} className="rounded-lg p-1 text-slate-400 hover:bg-slate-100"><X size={18}/></button>}</div><form onSubmit={submit} className="space-y-4"><label className="block"><span className="mb-1.5 block text-sm font-bold text-slate-700">Name</span><input value={form.data.name} onChange={e=>form.setData('name',e.target.value)} className="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm" autoFocus/>{form.errors.name&&<small className="text-rose-600">{form.errors.name}</small>}</label><label className="block"><span className="mb-1.5 block text-sm font-bold text-slate-700">Slug</span><input value={form.data.slug} onChange={e=>form.setData('slug',e.target.value)} placeholder="Auto-generated when blank" className="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm"/>{form.errors.slug&&<small className="text-rose-600">{form.errors.slug}</small>}</label><label className="block"><span className="mb-1.5 block text-sm font-bold text-slate-700">Description</span><textarea value={form.data.description} onChange={e=>form.setData('description',e.target.value)} rows="4" className="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm"/></label><button disabled={form.processing} className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-[#0d4c86] py-3 text-sm font-bold text-white"><Save size={16}/>{editing?'Save category':'Add category'}</button></form></section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><h2 className="font-extrabold text-slate-800">All categories <span className="ml-1 text-sm font-medium text-slate-400">({categories.length})</span></h2><FolderTree size={18} className="text-slate-400"/></div><div className="divide-y divide-slate-100">{categories.map(category=><article key={category.id} className="flex items-center gap-4 px-5 py-4 hover:bg-blue-50/35"><span className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-50 text-indigo-700"><FolderTree size={18}/></span><div className="min-w-0 flex-1"><strong className="block text-sm text-slate-800">{category.name}</strong><span className="block truncate text-xs text-slate-400">/{category.slug}{category.description ? ` · ${category.description}` : ''}</span></div><span className="hidden rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold text-slate-500 sm:block">{category.posts_count} posts</span><button onClick={()=>open(category)} className="grid h-9 w-9 place-items-center rounded-lg text-blue-700 hover:bg-blue-50"><Edit3 size={17}/></button>{['super_admin','admin'].includes(user?.role)&&<button onClick={()=>remove(category)} className="grid h-9 w-9 place-items-center rounded-lg text-rose-600 hover:bg-rose-50"><Trash2 size={17}/></button>}</article>)}{!categories.length&&<div className="px-5 py-16 text-center text-sm text-slate-400">Create a category to organise your posts.</div>}</div></section></div></>;
}
CategoriesIndex.layout=page=><AdminLayout>{page}</AdminLayout>;
